# Copy this file to .env.local (gitignored, never committed) and fill in
# the value after running `npx convex dev` for the first time — it prints
# (and auto-writes) the deployment URL, but VITE_CONVEX_URL specifically
# must be present for src/main.tsx to construct the ConvexReactClient.

VITE_CONVEX_URL=
